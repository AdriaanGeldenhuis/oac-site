Options -Indexes

# ===========================================
# SMART CACHING STRATEGY
# ===========================================

<IfModule mod_headers.c>
  # STATIC FILES - Cache for 1 day (Varnish + browser)
  # These rarely change, so caching = fast loading
  <FilesMatch "\.(jpg|jpeg|png|gif|webp|ico|svg|css|js|woff|woff2|ttf|eot)$">
    Header set Cache-Control "public, max-age=86400"
  </FilesMatch>

  # PHP FILES - Never cache (always fresh)
  # Dynamic content, user-specific data
  <FilesMatch "\.php$">
    Header set Cache-Control "private, no-cache, no-store, must-revalidate"
    Header set Pragma "no-cache"
    Header set Expires "0"
  </FilesMatch>

  # HTML FILES - Short cache (5 minutes)
  <FilesMatch "\.html$">
    Header set Cache-Control "public, max-age=300"
  </FilesMatch>
</IfModule>

# ===========================================
# SECURITY HEADERS
# ===========================================

<IfModule mod_headers.c>
  Header always set X-Frame-Options "DENY"
  Header always set X-Content-Type-Options "nosniff"
  Header always set Referrer-Policy "strict-origin-when-cross-origin"
  Header always set X-XSS-Protection "1; mode=block"
  Header always set Strict-Transport-Security "max-age=63072000; includeSubDomains; preload"
</IfModule>

# ===========================================
# PROTECT SENSITIVE FILES
# ===========================================

<FilesMatch "^(config|session|auth|csrf|headers|session_guard)\.php$">
  Order allow,deny
  Deny from all
</FilesMatch>

<FilesMatch "^config\.php$">
  Order Allow,Deny
  Deny from all
</FilesMatch>
